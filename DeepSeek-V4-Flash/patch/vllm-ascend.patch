diff --git a/vllm_ascend/device/device_op.py b/vllm_ascend/device/device_op.py
index abcdef12..34567890 100644
--- a/vllm_ascend/device/device_op.py
+++ b/vllm_ascend/device/device_op.py
@@ -768,7 +768,7 @@
     @staticmethod
     def dsa_kv_compress_scatter(cache, x, slot_mapping):
         """Scatter KV into cache. Non-A5: simple scatter of pre-quantized tensor."""
-        torch.ops._C_ascend.npu_scatter_nd_update_v2(cache, slot_mapping, x)
+        torch_npu.npu_scatter_nd_update_(cache, slot_mapping, x)

     # ===== Indexer Quant + Scatter =====

@@ -794,8 +794,8 @@
             kv_scale_out = kv_scale_out.unsqueeze(-1).to(torch.float16)
             if kv_scale_out.ndim < 4:
                 kv_scale_out = kv_scale_out.unsqueeze(-1)
-            torch.ops._C_ascend.npu_scatter_nd_update_v2(indexer_k_cache, slot_mapping, kv_out)
-            torch.ops._C_ascend.npu_scatter_nd_update_v2(indexer_scale_cache, slot_mapping, kv_scale_out)
+            torch_npu.npu_scatter_nd_update_(indexer_k_cache, slot_mapping, kv_out)
+            torch_npu.npu_scatter_nd_update_(indexer_scale_cache, slot_mapping, kv_scale_out)

         return q, q_scale, kv_out, kv_scale_out

@@ -808,7 +808,7 @@
             return None, None
         kv_out, kv_scale = torch_npu.npu_dynamic_quant(kv, dst_type=torch.int8)
         kv_scale = kv_scale.unsqueeze(-1)
-        torch.ops._C_ascend.npu_scatter_nd_update_v2(indexer_k_cache, slot_mapping, kv_out)
+        torch_npu.npu_scatter_nd_update_(indexer_k_cache, slot_mapping, kv_out)
         return kv_out, kv_scale

     @staticmethod
@@ -818,7 +818,7 @@
         kv_scale = kv_scale.to(torch.float16)
         if kv_scale.ndim < 4:
             kv_scale = kv_scale.unsqueeze(-1)
-        torch.ops._C_ascend.npu_scatter_nd_update_v2(indexer_scale_cache, slot_mapping, kv_scale)
+        torch_npu.npu_scatter_nd_update_(indexer_scale_cache, slot_mapping, kv_scale)

     @staticmethod
     def warmup_indexer_quant_scatter(hidden_states, slot_mapping):
@@ -831,8 +831,8 @@
         dummy_shape = (1, 1, 1, kv_dummy.shape[-1])
         indexer_k_cache = torch.zeros(dummy_shape, dtype=kv_dummy.dtype, device=hidden_states.device)
         indexer_scale_cache = torch.zeros(dummy_shape, dtype=torch.float16, device=hidden_states.device)
-        torch.ops._C_ascend.npu_scatter_nd_update_v2(indexer_k_cache, slot_mapping, kv_dummy)
-        torch.ops._C_ascend.npu_scatter_nd_update_v2(indexer_scale_cache, slot_mapping, kv_scale_dummy)
+        torch_npu.npu_scatter_nd_update_(indexer_k_cache, slot_mapping, kv_dummy)
+        torch_npu.npu_scatter_nd_update_(indexer_scale_cache, slot_mapping, kv_scale_dummy)

     # ===== Lightning Indexer Dtype Prep =====

diff --git a/vllm_ascend/models/deepseek_v4.py b/vllm_ascend/models/deepseek_v4.py
index b99fd94db..15a67ee42 100644
--- a/vllm_ascend/models/deepseek_v4.py
+++ b/vllm_ascend/models/deepseek_v4.py
@@ -24,6 +24,7 @@
 # limitations under the License.
 #
 import math
+import re
 import typing
 from collections.abc import Callable, Iterable
 from itertools import islice
@@ -297,6 +298,9 @@ def apply_rotary_emb(x: torch.Tensor, freqs_cis: torch.Tensor, inverse: bool = F
 def get_spec_layer_idx_from_weight_name(config: DeepseekV2Config | DeepseekV3Config, weight_name: str) -> int | None:
     if weight_name.startswith("mtp."):
         return 0
+    m = re.match(r"(?:model\.)?layers\.(\d+)\.", weight_name)
+    if m and int(m.group(1)) >= config.num_hidden_layers:
+        return int(m.group(1))
     return None
 
 
@@ -415,14 +419,14 @@ class DeepseekV4MoE(nn.Module):
 
         self.hash = layer_idx < config.num_hash_layers and not is_draft_layer
         if self.hash:
-            # Use zeros instead of empty to avoid garbage values causing
-            # invalid memory access in dummy mode (--load-format="dummy")
+            # MC2 dispatch/combine fails in the dummy profile run if a token
+            # repeats an expert id; give every row distinct experts instead of
+            # zeros.
+            token_ids = torch.arange(config.vocab_size, dtype=torch.int32).unsqueeze(1)
+            expert_offsets = torch.arange(config.num_experts_per_tok, dtype=torch.int32).unsqueeze(0)
+            token_to_expert = (token_ids + expert_offsets) % config.n_routed_experts
             self.gate.tid2eid = nn.Parameter(
-                torch.zeros(
-                    config.vocab_size,
-                    config.num_experts_per_tok,
-                    dtype=torch.int32,
-                ),
+                token_to_expert,
                 requires_grad=False,
             )
             self.gate.e_score_correction_bias = None
@@ -799,6 +803,7 @@ class DeepseekV4Attention(nn.Module):
             head_size=self.rope_head_dim,
             rotary_dim=self.rope_head_dim,
             max_position_embeddings=max_position_embeddings,
+            original_seq_len=config.rope_parameters["original_max_position_embeddings"] if self.compress_ratio > 1 else 0,
             is_neox_style=False,
             scaling_factor=config.rope_parameters["factor"],
             base=config.rope_parameters["rope_theta"],
@@ -926,7 +931,9 @@ class DeepseekV2DecoderLayer(nn.Module):
         parallel_config = vllm_config.parallel_config
 
         self.hidden_size = config.hidden_size
-        max_position_embeddings = config.rope_parameters["original_max_position_embeddings"]
+        # RoPE table spans the full yarn-extended context (original * factor);
+        # original_max_position_embeddings only drives the correction range.
+        max_position_embeddings = config.max_position_embeddings
         # DecoderLayers are created with `make_layers` which passes the prefix
         # with the layer's index.
         layer_idx = int(prefix.split(sep=".")[-1])
diff --git a/vllm_ascend/ops/rope_dsv4.py b/vllm_ascend/ops/rope_dsv4.py
index 8727abf4d..570c09236 100644
--- a/vllm_ascend/ops/rope_dsv4.py
+++ b/vllm_ascend/ops/rope_dsv4.py
@@ -15,11 +15,34 @@ class RopeGlobalState:
         self.spec_runtime_buffer: dict[str, dict[str, tuple[torch.Tensor, torch.Tensor]]] = {}
         self.layer_info: dict[str, tuple[str, list[str]]] = {}
         self.registry_summary: dict[str, set] = {}
+        # CPU master copies of full_rope_cache entries. The device-side cache
+        # lives in the CaMem pool and is discarded without backup by a level-2
+        # sleep; CPU memory is never touched by sleep, so these copies are the
+        # source of truth used by restore_rope_caches_after_sleep().
+        self.cpu_master: dict[str, tuple[torch.Tensor, torch.Tensor]] = {}
 
 
 _ROPE_STATE = RopeGlobalState()
 
 
+def restore_rope_caches_after_sleep():
+    """Restore device-side RoPE cos/sin tables from their CPU master copies.
+
+    Level-2 sleep releases the pool allocations backing full_rope_cache and
+    wake_up remaps them to zeroed pages. The tables are static content (they
+    depend only on the RoPE config, never on weights), so copying the CPU
+    masters back in place fully repairs them. The copy_() targets the existing
+    storage, keeping addresses stable for captured ACL graphs.
+    """
+    for config_key, (cpu_cos, cpu_sin) in _ROPE_STATE.cpu_master.items():
+        entry = _ROPE_STATE.full_rope_cache.get(config_key)
+        if entry is None:
+            continue
+        dev_cos, dev_sin = entry
+        dev_cos.copy_(cpu_cos)
+        dev_sin.copy_(cpu_sin)
+
+
 class RopeDataProxy:
     def __init__(self, data_map, is_cos=True):
         self._data = data_map
@@ -148,6 +171,7 @@ class ComplexExpRotaryEmbedding(nn.Module):
         head_size: int,
         rotary_dim: int,
         max_position_embeddings: int,
+        original_seq_len: int,
         base: int,
         scaling_factor: float,
         rope_groups: list[str] | None = None,
@@ -161,7 +185,7 @@ class ComplexExpRotaryEmbedding(nn.Module):
         beta_fast = extra_kwargs.get("beta_fast", 32)
         beta_slow = extra_kwargs.get("beta_slow", 1)
         config_key = (
-            f"rotary_dim{rotary_dim}_max_position_embeddings{max_position_embeddings}_"
+            f"rotary_dim{rotary_dim}_original_seq_len{original_seq_len}_"
             f"base{base}_scaling_factor{scaling_factor}_beta_fast{beta_fast}_beta_slow{beta_slow}"
         )
         _ROPE_STATE.layer_info[layername] = (config_key, rope_groups)
@@ -172,21 +196,25 @@ class ComplexExpRotaryEmbedding(nn.Module):
             _ROPE_STATE.registry_summary[config_key].add(grp)
 
         if config_key not in _ROPE_STATE.full_rope_cache:
-            inv_freq = self.precompute_freqs_cis(
-                rotary_dim, max_position_embeddings, max_position_embeddings, base, scaling_factor, beta_fast, beta_slow
+            complex_cis = self.precompute_freqs_cis(
+                rotary_dim, max_position_embeddings, original_seq_len, base, scaling_factor, beta_fast, beta_slow
             )
-            t = torch.arange(
-                max_position_embeddings * scaling_factor,
-                device=current_platform.device_type,
-                dtype=torch.float32,
-            )
-            freqs = torch.einsum("i,j -> ij", t, inv_freq)
-            cos = freqs.cos().repeat_interleave(2, dim=-1)
-            sin = freqs.sin().repeat_interleave(2, dim=-1)
+            # Tables stored fp32 (the bf16 cast was the dominant precision
+            # loss vs training); the fp64 angle computation above keeps
+            # them bit-identical to the training-side tables.
+            cos = complex_cis.real.float().repeat_interleave(2, dim=-1)
+            sin = complex_cis.imag.float().repeat_interleave(2, dim=-1)
             cos = cos.to(current_platform.device_type)
             sin = sin.to(current_platform.device_type)
 
             _ROPE_STATE.full_rope_cache[config_key] = (cos.unsqueeze(1).unsqueeze(1), sin.unsqueeze(1).unsqueeze(1))
+            if vllm_config is not None and vllm_config.model_config.enable_sleep_mode:
+                # Keep a CPU master copy: level-2 sleep discards the device
+                # table without backup; it is restored in place at wake_up.
+                _ROPE_STATE.cpu_master[config_key] = (
+                    cos.unsqueeze(1).unsqueeze(1).cpu(),
+                    sin.unsqueeze(1).unsqueeze(1).cpu(),
+                )
 
         use_eagle = (
             vllm_config is not None
@@ -220,52 +248,41 @@ class ComplexExpRotaryEmbedding(nn.Module):
 
     @staticmethod
     def precompute_freqs_cis(dim, seqlen, original_seq_len, base, factor, beta_fast, beta_slow):
-        def yarn_find_correction_dim(
-            num_rotations: int,
-            dim: int,
-            base: float = 10000,
-            max_position_embeddings: int = 2048,
-        ) -> float:
-            return (dim * math.log(max_position_embeddings / (num_rotations * 2 * math.pi))) / (2 * math.log(base))
-
-        # Find dim range bounds based on rotations
-        def yarn_find_correction_range(
-            low_rot: int,
-            high_rot: int,
-            dim: int,
-            base: float = 10000,
-            max_position_embeddings: int = 2048,
-            truncate: bool = True,
-        ) -> tuple[float | int, float | int]:
-            low = yarn_find_correction_dim(low_rot, dim, base, max_position_embeddings)
-            high = yarn_find_correction_dim(high_rot, dim, base, max_position_embeddings)
-            if truncate:
-                low = math.floor(low)
-                high = math.ceil(high)
-            return max(low, 0), min(high, dim - 1)  # Clamp values just in case
-
-        def yarn_linear_ramp_mask(low: float, high: float, dim: int, dtype: torch.dtype) -> torch.Tensor:
-            if low == high:
-                high += 0.001  # Prevent singularity
-
-            linear_func = (torch.arange(dim, dtype=dtype) - low) / (high - low)
-            ramp_func = torch.clamp(linear_func, 0, 1)
-            return ramp_func
-
-        pos_freqs = base ** (torch.arange(0, dim, 2, dtype=torch.float32) / dim)
-        inv_freq_extrapolation = 1.0 / pos_freqs
-        inv_freq_interpolation = 1.0 / (factor * pos_freqs)
-
-        low, high = yarn_find_correction_range(
-            beta_fast,
-            beta_slow,
-            dim,
-            base,
-            original_seq_len,
-        )
-        inv_freq_mask = (1 - yarn_linear_ramp_mask(low, high, dim // 2, dtype=torch.float32)) * 1
-        inv_freq = inv_freq_interpolation * (1 - inv_freq_mask) + inv_freq_extrapolation * inv_freq_mask
-        return inv_freq
+
+        def find_correction_dim(num_rotations, dim, base, max_seq_len):
+            return (dim * math.log(max_seq_len /
+                                   (num_rotations * 2 * math.pi)) /
+                    (2 * math.log(base)))
+
+        def find_correction_range(low_rot, high_rot, dim, base, max_seq_len):
+            low = math.floor(
+                find_correction_dim(low_rot, dim, base, max_seq_len))
+            high = math.ceil(
+                find_correction_dim(high_rot, dim, base, max_seq_len))
+            return max(low, 0), min(high, dim - 1)
+
+        def linear_ramp_factor(min_, max_, dim):
+            if min_ == max_:
+                max_ += 0.001
+            linear_func = (torch.arange(dim, dtype=torch.float64, device="cpu") -
+                           min_) / (max_ - min_)
+            return torch.clamp(linear_func, 0, 1)
+
+        # Angles are computed in float64: in fp32 the angle error grows
+        # linearly with position (2.4e-2 rad at 1M), wrecking long-context
+        # consistency; fp64 keeps it ~3e-8 and matches the training side
+        # (MindSpeed-LLM apply_deepseek4_rotary_embedding) bit for bit.
+        freqs = 1.0 / (base
+                       **(torch.arange(0, dim, 2, dtype=torch.float64, device="cpu") / dim))
+        if original_seq_len > 0:
+            low, high = find_correction_range(beta_fast, beta_slow, dim, base,
+                                              original_seq_len)
+            smooth = 1 - linear_ramp_factor(low, high, dim // 2)
+            freqs = freqs / factor * (1 - smooth) + freqs * smooth
+
+        t = torch.arange(seqlen, dtype=torch.float64, device="cpu")
+        freqs = torch.outer(t, freqs)
+        return torch.polar(torch.ones_like(freqs), freqs)
 
     def forward(
         self,
diff --git a/vllm_ascend/core/single_type_kv_cache_manager.py b/vllm_ascend/core/single_type_kv_cache_manager.py
index bc184c913..5553bbca8 100644
--- a/vllm_ascend/core/single_type_kv_cache_manager.py
+++ b/vllm_ascend/core/single_type_kv_cache_manager.py
@@ -46,9 +46,9 @@ class CompressAttentionManager(FullAttentionManager):
         # speculative decoding (MTP/EAGLE) with linear attention.
         # assert isinstance(self.kv_cache_spec, (CompressAttentionSpec, C4IndexerSpec))
 
-        num_tokens //= self.compress_ratio
-        num_tokens_main_model //= self.compress_ratio
-
+        num_tokens = cdiv(num_tokens, self.compress_ratio)
+        num_tokens_main_model = cdiv(num_tokens_main_model, self.compress_ratio)
+        
         return super().get_num_blocks_to_allocate(
             request_id,
             num_tokens,
@@ -141,9 +141,9 @@ class CompressAttentionManager(FullAttentionManager):
         Returns:
             The new allocated blocks.
         """
-        num_tokens //= self.compress_ratio
+        num_tokens = cdiv(num_tokens, self.compress_ratio)
         ## TODO: check spec decode
-        num_tokens_main_model //= self.compress_ratio
+        num_tokens_main_model = cdiv(num_tokens_main_model, self.compress_ratio)
 
         req_blocks = self.req_to_blocks[request_id]
         num_required_blocks = cdiv(num_tokens, self.block_size)
diff --git a/vllm_ascend/ops/fused_moe/token_dispatcher.py b/vllm_ascend/ops/fused_moe/token_dispatcher.py
index 1b6acbf8c..ef8897307 100644
--- a/vllm_ascend/ops/fused_moe/token_dispatcher.py
+++ b/vllm_ascend/ops/fused_moe/token_dispatcher.py
@@ -450,12 +450,24 @@ class TokenDispatcherWithAll2AllV(MoETokenDispatcher[MoEAllToAllCombineMetadata]
         self.num_local_experts = kwargs.get("num_local_experts", 0)
 
         assert self.num_local_experts > 0, "Expected at least one expert"
+        # Keep the routing table on CPU as the single source of truth.
+        # Level-2 sleep releases pooled NPU allocations without any backup, so
+        # a device-resident copy created here would hold garbage after wake_up,
+        # while CPU memory is never touched by sleep. A fresh device copy is
+        # made on every dispatch in _preprocess() -- the table is only
+        # num_experts * 4 bytes, and that path already performs H2D/D2H
+        # synchronizations per call (hence is never captured into ACL graphs),
+        # so the refresh costs nothing measurable.
         if self.num_local_experts > 1:
-            self.expert_ids_per_ep_rank = torch.tensor(
+            self.expert_ids_per_ep_rank_cpu = torch.tensor(
                 [i % self.num_local_experts for i in range(self.num_experts)],
                 dtype=torch.int32,
-                device=torch.npu.current_device(),
+                device="cpu",  # explicit: must stay off the NPU so sleep()
+                               # can never release it, regardless of any
+                               # global default-device setting
             )
+        else:
+            self.expert_ids_per_ep_rank_cpu = None
 
         local_expert_indices_offset = self.ep_rank * self.num_local_experts
 
@@ -621,7 +633,9 @@ class TokenDispatcherWithAll2AllV(MoETokenDispatcher[MoEAllToAllCombineMetadata]
             if num_global_tokens_per_local_expert is None:
                 raise ValueError("num_global_tokens_per_local_expert must be set before operations.")
             global_input_tokens_local_experts_indices = torch.repeat_interleave(
-                self.expert_ids_per_ep_rank, num_global_tokens_per_local_expert.ravel()
+                self.expert_ids_per_ep_rank_cpu.to(
+                    num_global_tokens_per_local_expert.device),
+                num_global_tokens_per_local_expert.ravel(),
             )
         else:
             torch.npu.synchronize()
diff --git a/vllm_ascend/worker/worker.py b/vllm_ascend/worker/worker.py
index 44be7c7d2..3ba42a1a4 100644
--- a/vllm_ascend/worker/worker.py
+++ b/vllm_ascend/worker/worker.py
@@ -223,6 +223,13 @@ class NPUWorker(WorkerBase):
         cleanup_enabled = getattr(get_ascend_config(), "enable_sleep_mode_extra_cleanup", False)
         if cleanup_enabled:
             self.sleep_wakeup_manager.sleep()
+        elif level == 2 and getattr(self.model_runner, "use_aclgraph", False):
+            # ACL graphs capture pointers into the CaMem pool, which level-2
+            # sleep releases; drop them so wake_up re-captures against the
+            # remapped pool. HCCL groups survive level-2 sleep fine and are
+            # left untouched (their destroy/restore path is not reliable).
+            self.sleep_wakeup_manager.acl_graph.sleep()
+            self._graphs_dropped_on_sleep = True
 
         allocator = CaMemAllocator.get_instance()
         allocator.sleep(offload_tags=("weights",) if level == 1 else tuple())
@@ -248,6 +255,14 @@ class NPUWorker(WorkerBase):
         allocator = CaMemAllocator.get_instance()
         allocator.wake_up(tags=tags)
 
+        # Level-2 sleep discards the pool-backed RoPE cos/sin tables held in
+        # rope_dsv4's module-global cache (deepseek_v4 DSA path). They are
+        # static, weight-independent content: restore them in place from the
+        # CPU master copies kept at model init.
+        from vllm_ascend.ops.rope_dsv4 import restore_rope_caches_after_sleep
+
+        restore_rope_caches_after_sleep()
+
         hidden_size = self.vllm_config.model_config.hf_text_config.hidden_size
         model = self.model_runner.model
         if self.vllm_config.quant_config is None and (tags is None or "weights" in tags):
@@ -278,6 +293,12 @@ class NPUWorker(WorkerBase):
         cleanup_enabled = getattr(get_ascend_config(), "enable_sleep_mode_extra_cleanup", False)
         if cleanup_enabled:
             self.sleep_wakeup_manager.wakeup(tags)
+        elif getattr(self, "_graphs_dropped_on_sleep", False):
+            # verl wakes in two stages (weights -> weight sync -> kv_cache);
+            # only recapture once everything is remapped.
+            if tags is None or "kv_cache" in tags:
+                self._graphs_dropped_on_sleep = False
+            self.sleep_wakeup_manager.acl_graph.wakeup(tags)
 
     def _check_weight_transfer_engine(self) -> None:
         if self.weight_transfer_engine is None:
@@ -608,6 +629,7 @@ class NPUWorker(WorkerBase):
             self.torch_allocated / GiB_bytes,
         )
 
+
     def execute_model(
         self,
         scheduler_output: "SchedulerOutput",
diff --git a/vllm_ascend/ops/linear.py b/vllm_ascend/ops/linear.py
index 859d51393..45356362c 100644
--- a/vllm_ascend/ops/linear.py
+++ b/vllm_ascend/ops/linear.py
@@ -37,7 +37,7 @@ from vllm.model_executor.layers.linear import (  # noqa
     UnquantizedLinearMethod,
 )
 from vllm.model_executor.layers.quantization.base_config import QuantizationConfig
-from vllm.model_executor.utils import set_weight_attrs
+from vllm.model_executor.utils import replace_parameter, set_weight_attrs
 from vllm.utils.torch_utils import direct_register_custom_op
 
 from vllm_ascend.ops.linear_op import get_parallel_op, get_replicated_op
@@ -89,7 +89,12 @@ class AscendUnquantizedLinearMethod(UnquantizedLinearMethod):
         # must use fp32 to avoid accuracy degradation in dsv4.
         if getattr(layer, "precast_fp32_weight", False):
             weight_fp32 = layer.weight.data.to(torch.float32)
-            layer.weight_fp32 = weight_fp32 if keep_nd_weight else maybe_trans_nz(weight_fp32)
+            new_fp32 = weight_fp32 if keep_nd_weight else maybe_trans_nz(weight_fp32)
+            # ACL graph holds weight_fp32 by reference. If a second
+            # after-loading (RL update_weights) replaces the reference, the
+            # captured graph still points at the old tensor and sampling would
+            # use stale weights; copy in place instead.
+            replace_parameter(layer, "weight_fp32", new_fp32, prefer_copy=True)
         if "conv1d" not in layer.prefix:
             # 310P torch_npu rejects FRACTAL_NZ matmul when the weight-side
             # matrix has n=1 or k=1. Keep scalar gates such as Qwen MoE's

--- a/vllm_ascend/ops/linear.py
+++ b/vllm_ascend/ops/linear.py
@@ -80,6 +80,13 @@
     return is_310p() and weight.ndim >= 2 and (weight.shape[-1] == 1 or weight.shape[-2] == 1)
 
 
+def _should_reshape_wo_a_to_3d(prefix: str) -> bool:
+    """Whether a DSV4 wo_a weight must be reshaped to
+    [n_local_groups, hidden_size, o_lora_rank] for npu_transpose_batchmatmul.
+    """
+    return "wo_a" in prefix and get_ascend_device_type() != AscendDeviceType.A5
+
+
 class AscendUnquantizedLinearMethod(UnquantizedLinearMethod):
     """Linear method without quantization"""
 
@@ -95,6 +102,15 @@
             # captured graph still points at the old tensor and sampling would
             # use stale weights; copy in place instead.
             replace_parameter(layer, "weight_fp32", new_fp32, prefer_copy=True)
+        # DSV4 wo_a is consumed by npu_transpose_batchmatmul in the 3D layout
+        # [n_local_groups, hidden_size, o_lora_rank]. Reshape it here so it
+        # applies to load-format=dummy too, where weight_loader never runs.
+        # Must happen before the NZ trans to keep the layout identical to the
+        # previous weight_loader-side reshape.
+        if _should_reshape_wo_a_to_3d(layer.prefix) and layer.weight.data.ndim == 2:
+            layer.weight.data = (
+                layer.weight.data.view(layer.n_local_groups, layer.o_lora_rank, -1).transpose(2, 1).contiguous()
+            )
         if "conv1d" not in layer.prefix:
             # 310P torch_npu rejects FRACTAL_NZ matmul when the weight-side
             # matrix has n=1 or k=1. Keep scalar gates such as Qwen MoE's
@@ -470,12 +486,12 @@
         return super().forward(input_)
 
     def weight_loader(self, param: Parameter, loaded_weight: torch.Tensor):
-        if "wo_a" in self.prefix and get_ascend_device_type() != AscendDeviceType.A5:
+        if _should_reshape_wo_a_to_3d(self.prefix):
             if self.weight.ndim == 2:
+                # Keep the raw 2D layout here. The 2D -> 3D reshape happens in
+                # process_weights_after_loading so it also runs for
+                # load-format=dummy, where weight_loader is never called.
                 super().weight_loader(param, loaded_weight)
-                self.weight.data = (
-                    self.weight.data.view(self.n_local_groups, self.o_lora_rank, -1).transpose(2, 1).contiguous()
-                )
             else:
                 # In RL update flows, wo_a can be loaded again after being
                 # transformed into [n_local_groups, hidden_size, o_lora_rank].
