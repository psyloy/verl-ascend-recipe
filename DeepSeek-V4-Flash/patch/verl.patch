diff --git a/verl/models/mcore/util.py b/verl/models/mcore/util.py
index 6a5541e7..0f567c18 100644
--- a/verl/models/mcore/util.py
+++ b/verl/models/mcore/util.py
@@ -682,6 +682,10 @@ def preprocess_bshd_engine(
     dense_shape = tuple(input_ids.shape[2:])
     seqlens_in_batch = input_ids.offsets().diff()
     max_seqlen = forced_max_seqlen if forced_max_seqlen is not None else seqlens_in_batch.max().item()
+    if is_npu_available:
+        # NPU kernels need max_seqlen 1024-aligned, floor 2048.
+        align_value = 1024
+        max_seqlen = max(((max_seqlen + align_value - 1) // align_value) * align_value, 2048)
     tp_size = mpu.get_tensor_model_parallel_world_size()
     # For CP (zigzag), sequence length must be divisible by (2 * cp_size).
     # After zigzag-CP split each rank holds s/cp_size tokens, which must also be
diff --git a/verl/third_party/vllm/__init__.py b/verl/third_party/vllm/__init__.py
index 505c9022..1e3bbfc4 100644
--- a/verl/third_party/vllm/__init__.py
+++ b/verl/third_party/vllm/__init__.py
@@ -12,6 +12,7 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+import os
 from importlib.metadata import PackageNotFoundError, version
 
 from packaging import version as vs
@@ -39,8 +40,8 @@ if package_version is None:
             f"vllm versions are 0.18.0+"
         )
 elif is_npu_available:
-    # sleep_mode=2 is not supported on vllm-ascend for now, will remove this restriction when this ability is ready.
-    VLLM_SLEEP_LEVEL = 1
+    # sleep_mode=2 stays opt-in via VLLM_SLEEP_LEVEL (validated for DSV4 RL colocate).
+    VLLM_SLEEP_LEVEL = int(os.environ.get("VLLM_SLEEP_LEVEL", "1"))
     from vllm import LLM
     from vllm.distributed import parallel_state
 elif vs.parse(package_version) >= vs.parse("0.18.0"):
diff --git a/verl/trainer/config/engine/mindspeed.yaml b/verl/trainer/config/engine/mindspeed.yaml
index 1435604f..0cda7371 100644
--- a/verl/trainer/config/engine/mindspeed.yaml
+++ b/verl/trainer/config/engine/mindspeed.yaml
@@ -4,43 +4,6 @@ _target_: verl.workers.config.MindSpeedEngineConfig
 # mindspeed_megatron or mindspeed_fsdp
 strategy: mindspeed_megatron
 
-# mindspeed_megatron backend config
-mcore_kwargs:
-  # llm model config
-  use_mcore_models: true
-  spec: []
-  qk_layernorm: true
-  position_embedding_type: rope
-  normalization: RMSNorm
-  disable_bias_linear: true
-  swiglu: true
-  attention_softmax_in_fp32: true
-  no_gradient_accumulation_fusion: true
-  group_query_attention: true
-
-  # only support transformer_engine for now
-  transformer_impl: transformer_engine
-
-  no_pad_to_seq_lengths: true
-  reset_attention_mask: true
-  context_parallel_algo: ulysses_cp_algo
-  attention_mask_type: general
-
-  # llm optimizer config
-  use_flash_attn: true
-  use_fused_rotary_pos_emb: true
-  sequence_parallel: true
-  use_rotary_position_embeddings: true
-  use_fused_swiglu: true
-  use_fused_rmsnorm: true
-  no_masked_softmax_fusion: true
-  use_distributed_optimizer: true
-
-  # llm train config
-  seq_length: 10240
-  micro_batch_size: 1
-  initial_loss_scale: 4096
-  init_method_std: 0.01
-  hidden_dropout: 0.0
+mcore_kwargs: {}
 
 fsdp_kwargs: {}
diff --git a/verl/utils/device.py b/verl/utils/device.py
index 33f0743f..a0b05fd0 100644
--- a/verl/utils/device.py
+++ b/verl/utils/device.py
@@ -284,6 +284,9 @@ def check_ipc_version_support(software_version: str, cann_version: str) -> bool:
     Raises:
         RuntimeError: If version format is invalid
     """
+    # A5 toolkits are not recognised by the version check below; force IPC on.
+    return True
+
     # For software_version like "25.3.rc1.2", "25.5.0", or "25.5.t3.b001",
     # we need to extract the base version
     # Use regex to extract version with the following rules:
diff --git a/verl/utils/vllm/npu_vllm_patch.py b/verl/utils/vllm/npu_vllm_patch.py
index 7467fc37..fd5e46d4 100644
--- a/verl/utils/vllm/npu_vllm_patch.py
+++ b/verl/utils/vllm/npu_vllm_patch.py
@@ -80,7 +80,14 @@ def apply_npu_vllm_patches() -> None:
         return
 
     # Disable flash_attn in RotaryEmbedding (NPU)
-    from vllm.model_executor.layers.fused_moe import FusedMoE
-
     patch_vllm013_rotary_emb()
-    _patch_legacy_fused_moe_weight_loader(FusedMoE)
+
+    # The FusedMoE transpose wrapper is only valid for legacy vLLM (<0.15);
+    # on newer vLLM it scrambles NPU MoE weights (train/rollout kl ~= 1.85 on DSV4).
+    import vllm
+    from packaging import version
+
+    if version.parse(vllm.__version__) < version.parse("0.15.0"):
+        from vllm.model_executor.layers.fused_moe import FusedMoE
+
+        _patch_legacy_fused_moe_weight_loader(FusedMoE)
diff --git a/verl/utils/vllm/patch.py b/verl/utils/vllm/patch.py
index 1e2224f6..ca64e3bf 100644
--- a/verl/utils/vllm/patch.py
+++ b/verl/utils/vllm/patch.py
@@ -73,6 +73,13 @@ try:
 except ImportError:
     pass
 
+try:
+    from vllm_ascend.models.deepseek_v4 import AscendDeepseekV4ForCausalLM
+
+    SUPPORTED_MOE_MODELS.append(AscendDeepseekV4ForCausalLM)
+except ImportError:
+    pass
+
 
 def patch_vllm_moe_model_weight_loader(model):
     # this is a work around to load the weight of vllm fused moe model
diff --git a/verl/workers/engine/mindspeed/transformer_impl.py b/verl/workers/engine/mindspeed/transformer_impl.py
index 6fa78d90..8b257ff5 100644
--- a/verl/workers/engine/mindspeed/transformer_impl.py
+++ b/verl/workers/engine/mindspeed/transformer_impl.py
@@ -34,7 +34,6 @@ from ..base import EngineRegistry
 from ..megatron import MegatronEngineWithLMHead, MegatronEngineWithValueHead
 from .utils import (
     apply_patch,
-    gpt_model_provider,
     reset_fp8_reuse_quantized_weight,
 )
 
@@ -140,6 +139,9 @@ class MindSpeedMegatronEngineWithLMHead(MegatronEngineWithLMHead):
         from megatron.core.enums import ModelType
         from megatron.training.training import get_model
 
+        # DSV4 uses its MindSpeed-LLM pretrain model_provider.
+        from mindspeed_llm.pretrain_deepseek4 import model_provider as gpt_model_provider
+
         # For forward_only, we don't need optimizer, lr_scheduler, checkpoint_mananager
         if self.engine_config.forward_only:
             module = get_model(gpt_model_provider, ModelType.encoder_or_decoder, wrap_with_ddp=False)
@@ -147,7 +149,7 @@ class MindSpeedMegatronEngineWithLMHead(MegatronEngineWithLMHead):
             module = get_model(gpt_model_provider, ModelType.encoder_or_decoder, wrap_with_ddp=True)
 
         if self.vanilla_bridge:
-            self.bridge.load_weights(module, self.model_config.local_path)
+            self.bridge.load_weights(module, self.model_config.local_path, memory_efficient=True)
         else:
             raise ValueError(f"vanilla_bridge should be true now, but got {self.vanilla_bridge}")
 
diff --git a/verl/workers/engine/mindspeed/utils.py b/verl/workers/engine/mindspeed/utils.py
index 42e949d4..38ac0480 100644
--- a/verl/workers/engine/mindspeed/utils.py
+++ b/verl/workers/engine/mindspeed/utils.py
@@ -153,10 +153,8 @@ def add_mcore_arguments(all_config: dict) -> dict:
 
 
 def apply_patch(model_config, engine_config, optimizer_config):
-    model_config = get_base_mcore_config_from_model_config(model_config)
-    optimizer_config = get_base_mcore_config_from_optim_config(optimizer_config)
     engine_config = get_base_mcore_config_from_engine_config(engine_config)
-    all_config = {**model_config, **optimizer_config, **engine_config}
+    all_config = {**engine_config}
     mcore_config = add_mcore_arguments(all_config)
     from mindspeed_llm.tasks.megatron_adaptor_v2 import repatch
 
diff --git a/verl/workers/rollout/vllm_rollout/bucketed_weight_transfer.py b/verl/workers/rollout/vllm_rollout/bucketed_weight_transfer.py
index 521d8907..08663a86 100644
--- a/verl/workers/rollout/vllm_rollout/bucketed_weight_transfer.py
+++ b/verl/workers/rollout/vllm_rollout/bucketed_weight_transfer.py
@@ -140,6 +140,10 @@ class BucketedWeightSender:
                     self._direct_send_large_weight(name, weight)
                     continue
 
+                # NPU IPC needs bucket offsets aligned to max(dtype width, 4).
+                alignment = max(weight.element_size(), 4)
+                if offset % alignment != 0:
+                    offset = (offset + alignment - 1) // alignment * alignment
                 bucket_meta[name] = {
                     "name": name,
                     "shape": weight.shape,
diff --git a/verl/workers/rollout/vllm_rollout/vllm_async_server.py b/verl/workers/rollout/vllm_rollout/vllm_async_server.py
index 55821b70..3cbb68c8 100644
--- a/verl/workers/rollout/vllm_rollout/vllm_async_server.py
+++ b/verl/workers/rollout/vllm_rollout/vllm_async_server.py
@@ -64,7 +64,11 @@ from verl.workers.rollout.vllm_rollout.utils import (
     get_vllm_max_lora_rank,
 )
 
-_VLLM_VERSION = version.parse(vllm.__version__)
+try:
+    _VLLM_VERSION = version.parse(vllm.__version__)
+except Exception:
+    # Custom vllm builds may have unparseable version strings.
+    _VLLM_VERSION = version.parse("0.23.0")
 
 if os.getenv("VERL_USE_GPT_OSS", "0") == "1":
     get_encoding()
@@ -1098,10 +1102,14 @@ class vLLMHttpServer:
         )
         # MTP drafter-only weights are initialized by vLLM and are not guaranteed
         # to be restored by actor weight sync after level 2 sleep discards them.
-        # lora only update adapter weights, so set sleep level to 1
-        # vllm_ascend not support sleep_level now. Enabling EP during training may lead to accuracy issues.
-        if mtp_rollout_enabled or self.lora_as_adapter or is_torch_npu_available(check_device=False):
+        # lora only update adapter weights, so set sleep level to 1.
+        # On NPU the level is configurable via VLLM_SLEEP_LEVEL (default 1).
+        if mtp_rollout_enabled or self.lora_as_adapter:
             sleep_level = 1
+        elif is_torch_npu_available(check_device=False):
+            from verl.third_party.vllm import VLLM_SLEEP_LEVEL
+
+            sleep_level = VLLM_SLEEP_LEVEL
         else:
             sleep_level = 2
         await self.engine.sleep(level=sleep_level)
@@ -1163,6 +1171,7 @@ class vLLMReplica(RolloutReplica):
             env_vars = {
                 **{var: "1" for var in get_platform().ray_noset_envvars()},
                 **get_platform().rollout_env_vars(),
+                "TASK_QUEUE_ENABLE": os.environ.get("VLLM_ASCEND_TASK_QUEUE_ENABLE", "0"),
             }
 
             server = self.server_class.options(
